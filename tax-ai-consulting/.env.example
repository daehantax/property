# 이 파일을 .env 로 복사한 뒤 실제 키를 넣으세요:
#   cp .env.example .env
#
# .env 는 .gitignore 에 등록되어 있어 git에 커밋되지 않습니다.
# 실제 API 키를 이 예시 파일이나 코드에 직접 적지 마세요.

# Anthropic API 키 (https://console.anthropic.com/settings/keys 에서 발급)
ANTHROPIC_API_KEY=sk-ant-여기에-실제-키-입력
